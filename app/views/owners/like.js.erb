$('.preached-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like_count')
    .html('<%= @owner.get_likes.size %> &#128591');
    $(this).closest('.preached-btn').html('Preached');
});

$('.preached-btn').on('ajax:success', function(){
    $(this).parent().parent().find('.like_count')
    .html('<%= @owner.get_likes.size %> &#128591');
    $(this).closet('.preached-btn').html('Preached');
});