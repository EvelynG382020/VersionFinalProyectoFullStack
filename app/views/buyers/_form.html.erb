<%= form_with(model: buyer, local: true) do |form| %>
  <% if buyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buyer.errors.count, "error") %> prohibited this buyer from being saved:</h2>

      <ul>
      <% buyer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Nombre" %>
    <%= form.text_field :name, placeholder: "Nombre y Apellidos"  %>
  </div>

  <div class="field">
    <%= form.label :phone, "Teléfono" %>
    <%= form.text_field :phone, placeholder: "+56912345678"  %>
  </div>

  <div class="field">
    <%= form.label :rut %>
    <%= form.text_field :rut, placeholder: "11.111.111-1" %>
  </div>

  <div class="field">
    <%= form.label :mail %>
    <%= form.text_field :mail, placeholder: "ejemplo@ejemplo.com" %>
  </div>

  <div class="field">
    <%= form.label :detail, "Observaciones" %>
    <%= form.text_area :detail%>
  </div>

  <div class="field">
    <%= form.label :owner_id, "Propietario" %>
    <%= form.collection_select :owner_id, Owner.all, :id, :name, prompt: true %>
  </div>

  <div class="field">
    <%= form.label :property_id, "Propiedad" %>
    <%= form.collection_select :property_id, Property.all, :id, :name, prompt: true %>
  </div>

  <div class="actions">
    <%= form.submit "Crear" %>
  </div>
<% end %>
