<p id="notice"><%= notice %></p>

<h1>Compradores</h1>

<div class="table-responsive">
  <table class="table">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Teléfono</th>
        <th>Rut</th>
        <th>Mail</th>
        <th>Observaciones</th>
        <th>Propietario</th>
        <th>Propiedad</th>
        <th>Fecha de ingreso</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @buyers.each do |buyer| %>
        <tr>
          <td><%= buyer.name %></td>
          <td><%= buyer.phone %></td>
          <td><%= buyer.rut %></td>
          <td><%= buyer.mail %></td>
          <td><%= buyer.detail %></td>
          <td><%= buyer.property.owner.name %></td>
          <td><%= buyer.property.name %></td>
          <td><%= buyer.created_at.to_datetime %></td>
          <td><%= link_to 'Mostrar', buyer %></td>
          <td><%= link_to 'Editar', edit_buyer_path(buyer) %></td>
          <td><%= link_to 'Eliminar', buyer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<%= link_to 'Nuevo Comprador', new_buyer_path %>
